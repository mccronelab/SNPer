dev/*
work/*
.nextflow/**/*
.nextflow*
test-out/*
.nf-test/
.snper_env/
.vscode/
out/
tests/test-out/
tests/work

# Pipeline output dirs (multiseg-out/ deliberately NOT ignored — it is the
# multi-segment test output currently under review, and only a few MB)
sarstest-out/
results/

# Large public FASTQ downloads (re-fetchable from SRA; test fixtures under
# data/fastq/ that are already tracked are unaffected by this rule)
data/fastq/SRR*.fastq.gz

# Reference downloads / build artifacts
data/reference/pr8.zip
data/reference/ncbi_dataset/
*.gff3_db

# Intermediate and debug artifacts
*.mpileup
*.tap
.nf-test.log
tests/.nf-test.log

# macOS
.DS_Store
